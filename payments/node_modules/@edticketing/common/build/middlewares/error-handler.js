"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.errorHandler = void 0;
var custom_error_1 = require("../errors/custom-error");
// a middleware to handle incoming error from user sign up. making use of a consistent strcuture of error among all file
exports.errorHandler = function (err, req, res, next) {
    if (err instanceof custom_error_1.CustomError) {
        return res.status(err.statusCode).send({ errors: err.serializeErrors() });
    }
    console.log(err);
    res.status(400).send({
        errors: [{ message: "something went wrong" }],
    });
};
// Common Response Structure when errors apprears
// {
//   errors: {
//     message: string, field?: string
//   }[]
// }
// -> this will fetch an array of object with the structure of {message: string, field?: string}
