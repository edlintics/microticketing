"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.currentUser = void 0;
var jsonwebtoken_1 = __importDefault(require("jsonwebtoken"));
exports.currentUser = function (req, res, next) {
    var _a;
    if (!((_a = req.session) === null || _a === void 0 ? void 0 : _a.jwt)) {
        // check if the the client fetch back the cookie and contain the jwt web token
        // the ? is to check if th req.session property exists
        // check if there is req.session, then if jwt is present
        return next();
    }
    try {
        var payload = jsonwebtoken_1.default.verify(
        // verify is a fucnction t decode jwt, and check if the token is present and not modifiy
        req.session.jwt, process.env.JWT_KEY);
        req.currentUser = payload;
    }
    catch (err) { } // if verify is not successful and throw back an error
    next();
};
